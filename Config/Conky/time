conky.config = {
    alignment = 'top_middle',
    background = false,
    use_xft = true,
    font = 'Ubuntu Mono:size=10:antialias=true',
    gap_x = 93,
    gap_y = $Y,
    maximum_width = 235,
    minimum_width = 235,
    extra_newline = false,
    double_buffer = true,
    own_window = true,
    own_window_class = 'ConkyTime',
    own_window_type = 'dock',
    update_interval = .5,
}

conky.text=[[${alignr}${execpi 10 ibam-custom.sh -c}$color|${color FCD862}${time %a %b %d %H:%M:%S}]]
